{
  "name": "kodify-poc",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "eslint src tests --ext js,ts --max-warnings 0",
    "start": "yarn run lint && yarn run test && node ./src",
    "test": "jest"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "sse-pubsub": "^1.4.1"
  },
  "devDependencies": {
    "eslint": "^7.19.0",
    "jest": "^26.6.3"
  },
  "eslintConfig": {
    "extends": "./config/.eslintrc.json"
  }
}
