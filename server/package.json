{
  "name": "kodify-poc",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "eslint src tests --ext js,ts --max-warnings 0",
    "build:clean": "node ./scripts/clean.js",
    "build:bundle": "node ./scripts/bundle.js",
    "build": "yarn run build:clean && yarn run lint & yarn run build:bundle && yarn run test",
    "start:server": "servor www index.js 2023 --reload --browse",
    "start": "yarn run build && yarn run start:server",
    "test": "jest"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "sse-pubsub": "^1.4.1"
  },
  "devDependencies": {
    "@microsoft/api-documenter": "^7.12.7",
    "@microsoft/api-extractor": "^7.13.1",
    "chokidar": "^3.5.3",
    "esbuild": "^0.15.7",
    "eslint": "^7.19.0",
    "jest": "^26.6.3",
    "servor": "^4.0.2"
  },
  "eslintConfig": {
    "extends": "./config/.eslintrc.json"
  }
}
