{
  "name": "kodify-poc-server",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "eslint src tests --ext js,ts --max-warnings 0",
    "start": "yarn run lint && yarn run test && node ./src",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "eslint": "^7.19.0",
    "jest": "^26.6.3"
  },
  "eslintConfig": {
    "extends": "./config/.eslintrc.json"
  }
}
